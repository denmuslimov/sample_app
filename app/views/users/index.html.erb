<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
	<%# @users.each do |user| %>
		<%# render user --> _user.htm.erb %>
		<%#= render user %>
	<%# end %>

	<%# render @users is a shorter way to do the same thing (uses _user.htm.erb) %>
	<%= render @users %>
</ul>

<%= will_paginate %>